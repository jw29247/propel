---
summary: "CLI reference for `propel uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `propel uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
propel uninstall
propel uninstall --all --yes
propel uninstall --dry-run
```
